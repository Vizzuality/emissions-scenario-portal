<ul>
  <% @models.each do |model| %>
  <li>
    <p><%= model.abbreviation %>. <%= model.full_name %></p>
    <p>Last updated on <%= model.updated_at.to_formatted_s(:short) %></p>
    <span><%= link_to 'SELECT THIS MODEL', model_url(model) %></span>
  </li>
  <% end %>
</ul>
